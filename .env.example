# URL del backend para el proxy de desarrollo (solo se usa en vite.config.js)
VITE_API_PROXY_TARGET=http://localhost:3000

# URL base del backend para build de producción (Railway)
# Ejemplo: https://mi-backend.up.railway.app
VITE_API_BASE_URL=
